//import { Avatar, AvatarImage } from "@/components/ui/avatar"

interface HeroProps {
  
};

export const Hero = ({
  
}: HeroProps) => {
  return (
    <>
      <h1>Hero Component</h1>
    </>
  )
};
